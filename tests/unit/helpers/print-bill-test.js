import { printBill } from '../../../helpers/print-bill';
import { module, test } from 'qunit';

module('Unit | Helper | print bill');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = printBill(42);
  assert.ok(result);
});
