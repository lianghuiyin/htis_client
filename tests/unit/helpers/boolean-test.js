import { boolean } from '../../../helpers/boolean';
import { module, test } from 'qunit';

module('Unit | Helper | boolean');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = boolean(42);
  assert.ok(result);
});
