import { reportCompute } from '../../../helpers/report-compute';
import { module, test } from 'qunit';

module('Unit | Helper | report compute');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reportCompute(42);
  assert.ok(result);
});
