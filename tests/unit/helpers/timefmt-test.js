import { timefmt } from '../../../helpers/timefmt';
import { module, test } from 'qunit';

module('Unit | Helper | timefmt');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = timefmt(42);
  assert.ok(result);
});
