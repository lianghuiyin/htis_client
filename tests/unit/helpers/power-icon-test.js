import { powerIcon } from '../../../helpers/power-icon';
import { module, test } from 'qunit';

module('Unit | Helper | power icon');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = powerIcon(42);
  assert.ok(result);
});
