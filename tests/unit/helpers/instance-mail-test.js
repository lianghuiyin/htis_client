import { instanceMail } from '../../../helpers/instance-mail';
import { module, test } from 'qunit';

module('Unit | Helper | instance mail');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = instanceMail(42);
  assert.ok(result);
});
