import { statusIcon } from '../../../helpers/status-icon';
import { module, test } from 'qunit';

module('Unit | Helper | status icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = statusIcon(42);
  assert.ok(result);
});
