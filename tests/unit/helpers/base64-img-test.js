import { base64Img } from '../../../helpers/base64-img';
import { module, test } from 'qunit';

module('Unit | Helper | base64 img');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = base64Img(42);
  assert.ok(result);
});
